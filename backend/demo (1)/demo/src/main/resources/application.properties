# Set to 'mysql' or 'postgres' to switch databases
spring.profiles.active=mysql

# Hibernate settings (creates/updates tables automatically based on your Java entities)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true